<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="mx.mc.mapper.TipoCancelacionMapper" >
 
    <select id="obtenerLista" resultType="mx.mc.model.TipoCancelacion" parameterType="mx.mc.model.TipoCancelacion" >
        SELECT idTipoCancelacion, descripcion, resurtir
        FROM tipoCancelacion
        WHERE activo=1;
    </select>
</mapper>    