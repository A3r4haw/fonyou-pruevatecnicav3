<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="mx.mc.mapper.EstatusFichaPrevencionMapper" >
 
    <select id="obtener" resultType="mx.mc.model.EstatusFichaPrevencion" parameterType="mx.mc.model.EstatusFichaPrevencion" >
        SELECT * FROM estatusFichaPrevencion
        WHERE 1=1
        <if test=" idEstatusPrevencion != null ">
            AND idEstatusPrevencion = #{ idEstatusPrevencion , jdbcType = INTEGER }
        </if>
        <if test=" descripcion != null ">
            AND descripcion = #{ descripcion , jdbcType = VARCHAR }
        </if>
        <if test=" activa != null ">
            AND activa = #{ activa , jdbcType = INTEGER }
        </if>
        LIMIT 1
    </select>
    
    <select id="obtenerLista" resultType="mx.mc.model.EstatusFichaPrevencion" parameterType="mx.mc.model.EstatusFichaPrevencion" >
        SELECT * FROM estatusFichaPrevencion
        WHERE 1=1
        <if test=" idEstatusPrevencion != null ">
            AND idEstatusPrevencion = #{ idEstatusPrevencion , jdbcType = INTEGER }
        </if>
        <if test=" descripcion != null ">
            AND descripcion = #{ descripcion , jdbcType = VARCHAR }
        </if>
        <if test=" activa != null ">
            AND activa = #{ activa , jdbcType = INTEGER }
        </if>
    </select>
    
</mapper>