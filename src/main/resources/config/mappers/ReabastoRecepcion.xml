<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="mx.mc.mapper.ReabastoRecepcionMapper" >

    <insert id="insertarListaReabastoRecepcion" parameterType="map" >
        <foreach collection="listReabastoRecepcion" item="item" separator=";">
            INSERT INTO reabastoRecepcion (
            <if test=" item.idReabastoRecepcion != null"> idReabastoRecepcion</if>
            <if test=" item.lote != null">, lote</if>
            <if test=" item.claveProveedor != null">, claveProveedor</if>
            <if test=" item.fechaCaducidad != null">, fechaCaducidad</if>
            <if test=" item.cantidad != null">, cantidad</if>
            <if test=" item.cantidadEnviada != null">, cantidadEnviada</if>
            <if test=" item.codigoBarras != null">, codigoBarras</if>
            <if test=" item.folioRecepcion != null">, folioRecepcion</if>
            <if test=" item.insertFecha != null">, insertFecha</if>
            <if test=" item.idUsuarioInsert != null">, idUsuarioInsert</if>
            ) VALUES (
            <if test=" item.idReabastoRecepcion != null"> #{ item.idReabastoRecepcion, jdbcType = VARCHAR }</if>
            <if test=" item.lote != null"> , #{ item.lote, jdbcType =  VARCHAR }</if>
            <if test=" item.claveProveedor != null"> , #{ item.claveProveedor, jdbcType = VARCHAR }</if>
            <if test=" item.fechaCaducidad != null"> , #{ item.fechaCaducidad, jdbcType = TIMESTAMP }</if>
            <if test=" item.cantidad != null"> , #{ item.cantidad, jdbcType = INTEGER }</if>
            <if test=" item.cantidadEnviada != null"> , #{ item.cantidadEnviada, jdbcType = INTEGER }</if>
            <if test=" item.codigoBarras != null"> , #{ item.codigoBarras, jdbcType = VARCHAR }</if>
            <if test=" item.folioRecepcion != null"> , #{ item.folioRecepcion, jdbcType = VARCHAR }</if>
            <if test=" item.insertFecha != null">, #{ item.insertFecha, jdbcType = TIMESTAMP }</if>
            <if test=" item.idUsuarioInsert != null">, #{ item.idUsuarioInsert, jdbcType = VARCHAR }</if>
            )
        </foreach>       
    </insert>

</mapper>