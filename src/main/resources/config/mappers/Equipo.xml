<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="mx.mc.mapper.EquipoMapper">

    <select id="obtenerByCampo" resultType="mx.mc.model.Equipo" parameterType="mx.mc.model.Equipo">
        SELECT idEquipo, nombre, ipEquipo, idTipoEquipo, idTipoSolucion, activa, insertFecha, insertIdUsuario, updateFecha, updateIdUsuario
        FROM equipo WHERE 1 = 1
        <if test=" idEquipo != null ">
            AND idEquipo = #{ idEquipo, jdbcType=VARCHAR}
        </if>
        <if test=" nombre != null ">
            AND nombre = #{ nombre, jdbcType=VARCHAR}
        </if>
        <if test=" ipEquipo != null ">
            AND ipEquipo = #{ ipEquipo, jdbcType=VARCHAR}
        </if>
        <if test=" idTipoEquipo != null ">
            AND idTipoEquipo = #{idTipoEquipo, jdbcType=INTEGER}
        </if>
        <if test=" idTipoSolucion != null ">
            AND idTipoSolucion = #{ idTipoSolucion, jdbcType=VARCHAR}
        </if>
        ;
    </select>

</mapper>