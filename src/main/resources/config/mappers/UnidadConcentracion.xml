<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="mx.mc.mapper.UnidadConcentracionMapper">

    <select id="obtener" resultType="mx.mc.model.UnidadConcentracion" parameterType="mx.mc.model.UnidadConcentracion">
        SELECT *
        FROM unidadConcentracion
        WHERE idUnidadConcentracion = #{ idUnidadConcentracion, jdbcType = INTEGER }
        LIMIT 1;
    </select>
    
    <select id="obtenerTodo" resultType="mx.mc.model.UnidadConcentracion" parameterType="mx.mc.model.UnidadConcentracion">
        SELECT idUnidadConcentracion, nombreUnidadConcentracion
        FROM unidadConcentracion
        WHERE 1=1
    </select>
    
    <select id="obtenerSiguienteId" parameterType="Map" resultType="Integer">
        SELECT MAX(idUnidadConcentracion) + 1 FROM unidadConcentracion;
    </select>
    
    <insert id="insertar" parameterType = "mx.mc.model.UnidadConcentracion">
        INSERT INTO unidadConcentracion (
            idUnidadConcentracion, nombreUnidadConcentracion, insertFecha, insertIdUsuario, updateFecha, updateIdUsuario
        ) VALUES (
            #{ idUnidadConcentracion, jdbcType = INTEGER }
            ,#{ nombreUnidadConcentracion, jdbcType = VARCHAR }
            ,#{ insertFecha, jdbcType = TIMESTAMP }
            ,#{ insertUsuarioId, jdbcType = VARCHAR }
            ,#{ updateFecha, jdbcType = TIMESTAMP }
            ,#{ updateUsuarioId, jdbcType = VARCHAR }
        ) 
    </insert>
    
    <select id="obtenerUnidadNombre" resultType="mx.mc.model.UnidadConcentracion" parameterType="Map">
        SELECT idUnidadConcentracion, nombreUnidadConcentracion
        FROM unidadConcentracion
        WHERE nombreUnidadConcentracion = #{nombreUnidadConcentracion, jdbcType = VARCHAR};
    </select>
    
</mapper>