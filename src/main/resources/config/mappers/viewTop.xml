<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="mx.mc.mapper.ViewTopMapper" >
 
    <select id="obtenerTopMedico" resultType="mx.mc.model.ViewTop" parameterType="mx.mc.model.ViewTop" >
        SELECT clave, nombre, numero FROM viewTopMedico
    </select>
 
    <select id="obtenerTopPaciente" resultType="mx.mc.model.ViewTop" parameterType="mx.mc.model.ViewTop" >
        SELECT clave, nombre, numero FROM viewTopPaciente
    </select>
 
    <select id="obtenerTopMedicamento" resultType="mx.mc.model.ViewTop" parameterType="mx.mc.model.ViewTop" >
        SELECT clave, nombre, numero FROM viewTopMedicamento
    </select>
 
    <select id="obtenerTopServicio" resultType="mx.mc.model.ViewTop" parameterType="mx.mc.model.ViewTop" >
        SELECT clave, nombre, numero FROM viewTopServicio
    </select>
 
    <select id="obtenerTopNivel" resultType="mx.mc.model.ViewTop" parameterType="mx.mc.model.ViewTop" >
        SELECT clave, nombre, numero FROM viewTopNivel
    </select>
    
</mapper>